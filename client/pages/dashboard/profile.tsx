import { Sidenav } from '@/components';

const profile = () => {
    return (
        <div className="min-h-screen bg-slate-200 flex">
            <Sidenav />
        </div>
    );
};

export default profile;
